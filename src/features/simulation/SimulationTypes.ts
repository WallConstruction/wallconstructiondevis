// src/features/simulation/SimulationTypes.ts
export interface SimulationState {
    name: string;
    email: string;
    phone: string;
    // Ajoutez toutes les propriétés nécessaires à la simulation
}